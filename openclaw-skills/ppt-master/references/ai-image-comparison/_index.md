# AI Image Comparison Fixtures

These images are upstream visual-reference fixtures for comparing rendering, palette, and composition types. They are not executable provider manifests in the OpenClaw package.

For new assets, follow [`../image-generator.md`](../image-generator.md): write a project-local manifest, call the current runtime image capability, and record the returned files with `image_manifest.py`.
