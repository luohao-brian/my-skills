[project]
name = "my-skills"
version = "0.1.0"
description = "Local OpenClaw skills and Hermes plugins"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32,<3",
]

[tool.uv]
package = false
