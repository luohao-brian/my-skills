diff --git a/scripts/tests/test_dogfood_leaves_fixes.py b/scripts/tests/test_dogfood_leaves_fixes.py
index 354d0dc..b25e62b 100644
--- a/scripts/tests/test_dogfood_leaves_fixes.py
+++ b/scripts/tests/test_dogfood_leaves_fixes.py
@@ -15,6 +15,7 @@ import tempfile
 import unittest
 import zipfile
 from pathlib import Path
+from unittest.mock import patch
 from xml.etree import ElementTree as ET
 
 SCRIPTS_DIR = Path(__file__).resolve().parents[1]
@@ -31,7 +32,7 @@ import web_to_md  # noqa: E402
 from svg_to_pptx.drawingml.converter import convert_svg_to_slide_shapes  # noqa: E402
 from svg_to_pptx.drawingml.utils import project_filter_errors  # noqa: E402
 from svg_to_pptx.pptx_package.builder import _slide_size_type  # noqa: E402
-from project_management.cli import _is_project_tree, PROJECTS_ROOT  # noqa: E402
+from project_management.cli import _is_project_tree  # noqa: E402
 from narration_sync import _project_input_path  # noqa: E402
 from tts_backends import backend_edge  # noqa: E402
 from compact_svg_styles import compact_svg_style_tree  # noqa: E402
@@ -129,8 +130,7 @@ class PdfClauseAndScanTests(unittest.TestCase):
 
 class ImportSourcesProjectTreeTests(unittest.TestCase):
     def test_research_web_sources_dir_is_not_a_project(self) -> None:
-        PROJECTS_ROOT.mkdir(parents=True, exist_ok=True)
-        with tempfile.TemporaryDirectory(dir=PROJECTS_ROOT) as tmp:
+        with tempfile.TemporaryDirectory() as tmp:
             root = Path(tmp)
             scratch = root.with_name(root.name + "_web_sources")
             scratch.mkdir()
@@ -140,7 +140,8 @@ class ImportSourcesProjectTreeTests(unittest.TestCase):
                 (root / "svg_output").mkdir()
                 (root / "sources").mkdir()
                 (root / "sources" / "a.md").write_text("# a\n", encoding="utf-8")
-                self.assertTrue(_is_project_tree(root / "sources" / "a.md"))
+                with patch("project_management.cli.PROJECTS_ROOT", root.parent):
+                    self.assertTrue(_is_project_tree(root / "sources" / "a.md"))
             finally:
                 for child in scratch.iterdir():
                     child.unlink()
diff --git a/scripts/tests/test_preview_browser.py b/scripts/tests/test_preview_browser.py
index b94784b..80d999e 100644
--- a/scripts/tests/test_preview_browser.py
+++ b/scripts/tests/test_preview_browser.py
@@ -114,4 +114,3 @@ class PreviewBrowserTests(unittest.TestCase):
             with patch.object(common.webbrowser, 'open', return_value=False):
                 with self.assertLogs('server_common', level='WARNING'):
                     self.assertFalse(common.open_preview_browser(URL))
-
diff --git a/scripts/tests/test_source_to_md_cli.py b/scripts/tests/test_source_to_md_cli.py
index 4ad0d9a..61f86c3 100644
--- a/scripts/tests/test_source_to_md_cli.py
+++ b/scripts/tests/test_source_to_md_cli.py
@@ -254,7 +254,7 @@ class SourceCollisionTests(unittest.TestCase):
     def setUp(self) -> None:
         self.temporary = tempfile.TemporaryDirectory()
         self.addCleanup(self.temporary.cleanup)
-        self.root = Path(self.temporary.name)
+        self.root = Path(self.temporary.name).resolve()
 
     def _run_cli(self, *arguments: str) -> subprocess.CompletedProcess:
         return subprocess.run(
diff --git a/scripts/tests/test_template_flow_batch1.py b/scripts/tests/test_template_flow_batch1.py
index fcaae94..b7ab3f9 100644
--- a/scripts/tests/test_template_flow_batch1.py
+++ b/scripts/tests/test_template_flow_batch1.py
@@ -81,7 +81,7 @@ class TemplateInstallTests(unittest.TestCase):
     def setUp(self) -> None:
         temp = tempfile.TemporaryDirectory()
         self.addCleanup(temp.cleanup)
-        self.tmp = Path(temp.name)
+        self.tmp = Path(temp.name).resolve()
         self.project = _workspace(self.tmp / 'project', 'deck', 'old')
         self.layout = _workspace(self.tmp / 'layout', 'layout', 'new')
         for directory in ('images', 'icons'):
diff --git a/scripts/tests/test_template_flow_batch3.py b/scripts/tests/test_template_flow_batch3.py
index 527b10b..75dfa70 100644
--- a/scripts/tests/test_template_flow_batch3.py
+++ b/scripts/tests/test_template_flow_batch3.py
@@ -94,7 +94,7 @@ class TemplateFlowBatch3Tests(unittest.TestCase):
     def setUp(self) -> None:
         temp = tempfile.TemporaryDirectory()
         self.addCleanup(temp.cleanup)
-        self.root = Path(temp.name)
+        self.root = Path(temp.name).resolve()
 
     def test_a_mirror_keeps_zero_height_source_line(self) -> None:
         empty = ET.fromstring(f'<svg xmlns="{SVG_NS}"/>')
diff --git a/scripts/tests/test_web_to_md_security.py b/scripts/tests/test_web_to_md_security.py
index acf4b83..77e61bc 100644
--- a/scripts/tests/test_web_to_md_security.py
+++ b/scripts/tests/test_web_to_md_security.py
@@ -363,7 +363,7 @@ class TransportBindingTests(unittest.TestCase):
                     patch.dict(web_to_md.os.environ, HTTP_PROXY='http://127.0.0.1:9',
                                HTTPS_PROXY='http://127.0.0.1:9', ALL_PROXY='http://127.0.0.1:9'), \
                     patch('urllib3.connection._ssl_wrap_socket_and_match_hostname',
-                          return_value=SimpleNamespace(socket=sock, is_verified=True)) as tls:
+                          return_value=(sock, True)) as tls:
                 response = web_to_md._http_get(f'{scheme}://public.example/')
                 self.assertEqual(response.content, b'public')
                 sock.connect.assert_called_once_with(('8.8.8.8', 443 if scheme == 'https' else 80))
