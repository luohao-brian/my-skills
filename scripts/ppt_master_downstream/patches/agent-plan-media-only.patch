diff --git a/scripts/image_gen.py b/scripts/image_gen.py
--- a/scripts/image_gen.py
+++ b/scripts/image_gen.py
@@ -2,46 +2,9 @@
 """
 Unified Image Generation Tool

-Dispatches to the appropriate backend based on provider configuration.
-
-Backend selection (`IMAGE_BACKEND` in `.env` or the current process environment):
-  IMAGE_BACKEND=gemini      -> Gemini backend (google-genai SDK)
-  IMAGE_BACKEND=openai      -> OpenAI-compatible backend (raw HTTP via requests)
-  IMAGE_BACKEND=minimax     -> MiniMax image backend
-  IMAGE_BACKEND=stability   -> Stability AI backend
-  IMAGE_BACKEND=bfl         -> Black Forest Labs FLUX backend
-  IMAGE_BACKEND=ideogram    -> Ideogram backend
-  IMAGE_BACKEND=qwen        -> Alibaba Qwen image backend
-  IMAGE_BACKEND=zhipu       -> Zhipu GLM-Image backend
-  IMAGE_BACKEND=volcengine  -> Volcengine Seedream backend
-  IMAGE_BACKEND=ark-api     -> Normal Ark API profile via Volcengine backend
-  IMAGE_BACKEND=ark-agent-plan -> Ark Agent Plan profile via Volcengine backend
-  IMAGE_BACKEND=modelscope  -> ModelScope backend
-  IMAGE_BACKEND=siliconflow -> SiliconFlow backend
-  IMAGE_BACKEND=fal         -> fal.ai backend
-  IMAGE_BACKEND=replicate   -> Replicate backend
-  IMAGE_BACKEND=openrouter  -> OpenRouter backend
-
-Configuration source (process env wins, `.env` is the fallback layer):
-  1. Current process environment variables
-  2. The first `.env` found among:
-     - Current working directory
-     - Skill directory (e.g. `~/.agents/{baseDir}/.env`)
-     - Repo root (when running from a clone)
-     - `~/.ppt-master/.env` (user-level config)
-
-Supported keys:
-  IMAGE_BACKEND    optional explicit backend override
-
-  A caller provider bridge may supply IMAGE_GEN_PROVIDER, IMAGE_GEN_BASEURL,
-  and IMAGE_GEN_MODEL_ID. The Volcengine backend is selected
-  automatically for volc-ark and volc-ark-agent-plan; the latter is the default.
-
-  Provider-specific keys are used for credentials and overrides, for example:
-    GEMINI_API_KEY / GEMINI_MODEL / GEMINI_BASE_URL
-    OPENAI_API_KEY / OPENAI_MODEL / OPENAI_BASE_URL
-    QWEN_API_KEY / QWEN_MODEL / QWEN_BASE_URL
-    ZHIPU_API_KEY / ZHIPU_MODEL / ZHIPU_BASE_URL
+This distribution fixes image generation to the Ark Agent Plan profile through
+the upstream Volcengine backend. The only credential is
+`ARK_AGENT_PLAN_API_KEY`; backend selection is intentionally unavailable.

 Usage:
   python3 image_gen.py "prompt" --aspect_ratio 16:9 --image_size 1K -o images/
@@ -67,31 +30,7 @@ from config import load_prefixed_env_file, resolve_env_path
 configure_utf8_stdio()

 ENV_PATH = resolve_env_path()
-IMAGE_ENV_PREFIXES = (
-    "IMAGE_",
-    "GEMINI_",
-    "OPENAI_",
-    "MINIMAX_",
-    "STABILITY_",
-    "BFL_",
-    "IDEOGRAM_",
-    "QWEN_",
-    "DASHSCOPE_",
-    "ZHIPU_",
-    "BIGMODEL_",
-    "VOLCENGINE_",
-    "ARK_",
-    "MODELSCOPE_",
-    "SILICONFLOW_",
-    "FAL_",
-    "REPLICATE_",
-    "OPENROUTER_",
-)
-DEPRECATED_IMAGE_KEYS = {
-    "IMAGE_API_KEY",
-    "IMAGE_MODEL",
-    "IMAGE_BASE_URL",
-}
+IMAGE_ENV_PREFIXES = ("ARK_AGENT_PLAN_", "IMAGE_CONCURRENCY")

 # All aspect ratios accepted by the unified CLI
 # (each backend validates its own subset internally)
@@ -149,8 +88,8 @@ BACKEND_REGISTRY = {
         "tier": "core",
         "label": "Volcengine Seedream",
         "default_model": "doubao-seedream-5.0-lite",
-        "key_hint": "ARK_AGENT_PLAN_API_KEY / ARK_API_KEY / VOLCENGINE_API_KEY",
-        "aliases": ["ark", "ark-api", "ark-agent-plan", "doubao", "seedream"],
+        "key_hint": "ARK_AGENT_PLAN_API_KEY",
+        "aliases": ["ark-agent-plan", "doubao", "seedream"],
     },
     "modelscope": {
         "module": "backend_modelscope",
@@ -217,53 +156,16 @@ BACKEND_REGISTRY = {

 TIER_ORDER = {"core": 0, "extended": 1, "experimental": 2}
 SUPPORTED_BACKENDS = tuple(sorted(BACKEND_REGISTRY))
-IMAGE_BACKEND_PROFILES = {
-    "ark-api": "volcengine",
-    "ark-agent-plan": "volcengine",
-}
-IMAGE_PROVIDER_BACKENDS = {
-    "volc-ark": "volcengine",
-    "volc-ark-agent-plan": "volcengine",
-}
-IMAGE_BACKEND_CHOICES = tuple(sorted(set(SUPPORTED_BACKENDS) | set(IMAGE_BACKEND_PROFILES)))
+FIXED_IMAGE_BACKEND = "volcengine"


 def _load_image_env_file() -> None:
-    """
-    Load image generation config from the resolved `.env` as a fallback layer.
-
-    Existing process environment variables win over `.env`.
-    """
-    replacements = {
-        "IMAGE_API_KEY": "GEMINI_API_KEY / OPENAI_API_KEY / QWEN_API_KEY / ZHIPU_API_KEY / ...",
-        "IMAGE_MODEL": "GEMINI_MODEL / OPENAI_MODEL / QWEN_MODEL / ZHIPU_MODEL / ...",
-        "IMAGE_BASE_URL": "GEMINI_BASE_URL / OPENAI_BASE_URL / QWEN_BASE_URL / ZHIPU_BASE_URL / ...",
-    }
-    deprecated_messages = {
-        key: (
-            "Global image config keys have been removed.\n"
-            f"Use IMAGE_BACKEND plus provider-specific keys instead, such as {replacement}."
-        )
-        for key, replacement in replacements.items()
-    }
-    load_prefixed_env_file(IMAGE_ENV_PREFIXES, deprecated_keys=deprecated_messages)
+    """Load only the fixed Agent Plan credential and operational concurrency."""
+    load_prefixed_env_file(IMAGE_ENV_PREFIXES)


 def _validate_runtime_config() -> None:
-    """Reject deprecated global image variables from any configuration source."""
-    for key in DEPRECATED_IMAGE_KEYS:
-        if key not in os.environ:
-            continue
-        replacement = {
-            "IMAGE_API_KEY": "GEMINI_API_KEY / OPENAI_API_KEY / QWEN_API_KEY / ZHIPU_API_KEY / ...",
-            "IMAGE_MODEL": "GEMINI_MODEL / OPENAI_MODEL / QWEN_MODEL / ZHIPU_MODEL / ...",
-            "IMAGE_BASE_URL": "GEMINI_BASE_URL / OPENAI_BASE_URL / QWEN_BASE_URL / ZHIPU_BASE_URL / ...",
-        }[key]
-        raise ValueError(
-            f"Unsupported image config key: {key}\n"
-            "Global image config keys have been removed.\n"
-            f"Use IMAGE_BACKEND plus provider-specific keys instead, such as {replacement}."
-        )
+    """The fixed backend ignores unrelated image-provider configuration."""


 def _build_backend_aliases() -> dict[str, str]:
@@ -303,47 +205,19 @@ def _load_backend(canonical_name: str) -> tuple[object, str]:


 def _print_backend_list() -> None:
-    """Print supported backends grouped by support tier."""
-    print("Supported image backends:\n")
-    tiers = ("core", "extended", "experimental")
-    for tier in tiers:
-        print(f"{tier.upper()}:")
-        for name, info in sorted(
-            BACKEND_REGISTRY.items(),
-            key=lambda item: (TIER_ORDER[item[1]["tier"]], item[0]),
-        ):
-            if info["tier"] != tier:
-                continue
-            print(
-                f"  {name:<12} {info['label']} | default={info['default_model']} | keys={info['key_hint']}"
-            )
-        print()
-    print("Recommendation: prefer CORE backends for everyday PPT generation.")
-    print(f"Config fallback file: {ENV_PATH}")
+    """Print the single supported downstream image profile."""
+    print("Supported image backend: ark-agent-plan")
+    print("Credential: ARK_AGENT_PLAN_API_KEY")


 def _resolve_backend() -> tuple[object, str]:
     """
-    Determine which backend to use from explicit configuration.
+    Return the fixed Ark Agent Plan implementation.

     Returns:
         A backend module with a generate() function.
     """
-    backend_name = os.environ.get("IMAGE_BACKEND", "").strip().lower()
-    if backend_name:
-        canonical = BACKEND_ALIASES.get(backend_name)
-        if not canonical:
-            supported = ", ".join(SUPPORTED_BACKENDS)
-            print(f"Error: Unknown IMAGE_BACKEND='{backend_name}'. Supported: {supported}")
-            sys.exit(1)
-        return _load_backend(canonical)
-
-    provider = os.environ.get("IMAGE_GEN_PROVIDER", "").strip().lower()
-    provider_backend = IMAGE_PROVIDER_BACKENDS.get(provider)
-    if provider_backend:
-        return _load_backend(provider_backend)
-
-    return _load_backend("volcengine")
+    return _load_backend(FIXED_IMAGE_BACKEND)


 _AI_IMAGE_PATH_ROW_RE = re.compile(
@@ -1138,10 +1012,6 @@ def main() -> None:
         "--model", "-m", default=None,
         help="Model name. Default depends on backend."
     )
-    parser.add_argument(
-        "--backend", "-b", default=None, choices=IMAGE_BACKEND_CHOICES,
-        help="Override IMAGE_BACKEND env var."
-    )
     parser.add_argument(
         "--list-backends", action="store_true",
         help="List available backends grouped by support tier and exit."
@@ -1257,10 +1127,6 @@ def main() -> None:
         print(f"Error: {e}")
         sys.exit(1)

-    # CLI --backend overrides the value loaded from .env
-    if args.backend:
-        os.environ["IMAGE_BACKEND"] = args.backend
-
     backend, backend_name = _resolve_backend()
     print(f"Using backend: {backend_name}\n")

diff --git a/scripts/image_backends/backend_volcengine.py b/scripts/image_backends/backend_volcengine.py
--- a/scripts/image_backends/backend_volcengine.py
+++ b/scripts/image_backends/backend_volcengine.py
@@ -3,12 +3,7 @@
 Volcengine Seedream image generation backend.

 Configuration keys:
-  ARK_AGENT_PLAN_API_KEY             (Agent Plan profile)
-  ARK_API_KEY                        (Ark API profile)
-  IMAGE_GEN_PROVIDER / IMAGE_GEN_BASEURL / IMAGE_GEN_MODEL_ID
-                                     (optional caller provider bridge)
-  VOLCENGINE_API_KEY                 (legacy upstream fallback)
-  VOLCENGINE_BASE_URL / VOLCENGINE_MODEL (legacy optional overrides)
+  ARK_AGENT_PLAN_API_KEY             (required)
 """

 import sys
@@ -24,10 +19,9 @@ configure_utf8_stdio()

 if __name__ == "__main__":
     print(__doc__)
-    print("Use via: python3 {baseDir}/scripts/image_gen.py \"prompt\" --backend volcengine")
+    print("Use via: python3 {baseDir}/scripts/image_gen.py \"prompt\"")
     raise SystemExit(0 if any(arg in {"-h", "--help", "help"} for arg in sys.argv[1:]) else 1)

-import os
 import time

 import requests
@@ -44,12 +38,10 @@ from image_backends.backend_common import (
 )


-DEFAULT_ENDPOINT = "https://operator.las.cn-beijing.volces.com/api/v1/images/generations"
-DEFAULT_MODEL = "doubao-seedream-4-5-251128"
-ARK_API_ENDPOINT = "https://ark.cn-beijing.volces.com/api/v3"
-ARK_API_MODEL = "doubao-seedream-5-0-260128"
 ARK_AGENT_PLAN_ENDPOINT = "https://ark.cn-beijing.volces.com/api/plan/v3"
 ARK_AGENT_PLAN_MODEL = "doubao-seedream-5.0-lite"
+FIXED_PROFILE = "ark-agent-plan"
+FIXED_PROVIDER = "volc-ark-agent-plan"

 ARK_ASPECT_RATIO_SIZE_MAP = {
     "1:1": (2048, 2048),
@@ -70,36 +62,6 @@ ARK_SIZE_SCALE = {
     "4K": 1.0,
 }

-# IMAGE_BACKEND selects a profile; every profile resolves to the canonical
-# key/base_url/model inputs consumed by this single upstream backend.
-IMAGE_BACKEND_PROFILES = {
-    "ark-api": {
-        "provider": "volc-ark",
-        "key_envs": ("ARK_API_KEY",),
-        "base_url": ARK_API_ENDPOINT,
-        "model": ARK_API_MODEL,
-        "ark_api": True,
-    },
-    "ark-agent-plan": {
-        "provider": "volc-ark-agent-plan",
-        "key_envs": ("ARK_AGENT_PLAN_API_KEY",),
-        "base_url": ARK_AGENT_PLAN_ENDPOINT,
-        "model": ARK_AGENT_PLAN_MODEL,
-        "ark_api": True,
-    },
-    "volcengine": {
-        "provider": "volcengine",
-        "key_envs": ("VOLCENGINE_API_KEY",),
-        "base_url": DEFAULT_ENDPOINT,
-        "model": DEFAULT_MODEL,
-        "ark_api": False,
-    },
-}
-IMAGE_PROVIDER_PROFILES = {
-    "volc-ark": "ark-api",
-    "volc-ark-agent-plan": "ark-agent-plan",
-}
-
 ASPECT_RATIO_SIZE_MAP = {
     "512px": {
         "1:1": "1024x1024",
@@ -189,7 +151,8 @@ def _resolve_size(aspect_ratio: str, image_size: str, *, ark_api: bool = False)
 def _generate_image(api_key: str, prompt: str,
                     aspect_ratio: str = "1:1", image_size: str = "1K",
                     output_dir: str = None, filename: str = None,
-                    model: str = DEFAULT_MODEL, base_url: str = DEFAULT_ENDPOINT,
+                    model: str = ARK_AGENT_PLAN_MODEL,
+                    base_url: str = ARK_AGENT_PLAN_ENDPOINT,
                     ark_api: bool = False) -> str:
     """Generate one image with the Volcengine backend."""
     size = _resolve_size(aspect_ratio, image_size, ark_api=ark_api)
@@ -232,41 +195,19 @@ def _generate_image(api_key: str, prompt: str,


 def _resolve_profile(model: str = None) -> dict[str, object]:
-    """Resolve IMAGE_BACKEND to canonical Volcengine key/base URL/model inputs."""
-    profile_name = os.environ.get("IMAGE_BACKEND", "").strip().lower()
-    if profile_name not in IMAGE_BACKEND_PROFILES:
-        provider = os.environ.get("IMAGE_GEN_PROVIDER", "").strip().lower()
-        profile_name = IMAGE_PROVIDER_PROFILES.get(provider, "")
-
-    if not profile_name:
-        profile_name = "ark-agent-plan"
-
-    profile = IMAGE_BACKEND_PROFILES[profile_name]
-    key_envs = tuple(str(name) for name in profile["key_envs"])
+    """Resolve the fixed Ark Agent Plan image profile."""
     api_key = require_api_key(
-        *key_envs,
-        message=(
-            f"No image key found for IMAGE_BACKEND={profile_name}. "
-            f"Configure {' or '.join(key_envs)}."
-        ),
+        "ARK_AGENT_PLAN_API_KEY",
+        message="No Agent Plan image key found. Configure ARK_AGENT_PLAN_API_KEY.",
     )

     return {
-        "profile": profile_name,
-        "provider": profile["provider"],
+        "profile": FIXED_PROFILE,
+        "provider": FIXED_PROVIDER,
         "api_key": api_key,
-        "base_url": (
-            os.environ.get("IMAGE_GEN_BASEURL")
-            or os.environ.get("VOLCENGINE_BASE_URL")
-            or profile["base_url"]
-        ),
-        "model": (
-            model
-            or os.environ.get("IMAGE_GEN_MODEL_ID")
-            or os.environ.get("VOLCENGINE_MODEL")
-            or profile["model"]
-        ),
-        "ark_api": profile["ark_api"],
+        "base_url": ARK_AGENT_PLAN_ENDPOINT,
+        "model": model or ARK_AGENT_PLAN_MODEL,
+        "ark_api": True,
     }


diff --git a/scripts/notes_to_audio.py b/scripts/notes_to_audio.py
--- a/scripts/notes_to_audio.py
+++ b/scripts/notes_to_audio.py
@@ -1,32 +1,17 @@
 #!/usr/bin/env python3
 """Generate per-slide narration audio from PPT Master notes.

-This script uses provider backends for the same per-slide output contract on
-macOS, Linux, and Windows. `edge-tts` remains the default no-key backend and
-also writes one compact, word-timed SRT file per slide from the same TTS stream.
-MiniMax and CosyVoice request word timings; ElevenLabs requests character
-alignment. All four apply the same compact, text-faithful cue regrouping.
-Qwen remains audio-only because its current TTS API exposes no timestamps.
+This distribution fixes narration to Ark Agent Plan through the Volcengine
+Seed-TTS adapter. Backend selection is intentionally unavailable.

 Usage:
-    python3 {baseDir}/scripts/notes_to_audio.py <project_path> --voice zh-CN-XiaoxiaoNeural
-    python3 {baseDir}/scripts/notes_to_audio.py <project_path> --provider elevenlabs --voice-id <voice_id>
-    python3 {baseDir}/scripts/notes_to_audio.py <project_path> --provider minimax --voice-id <voice_id>
-    python3 {baseDir}/scripts/notes_to_audio.py <project_path> --provider qwen --voice-id <voice>
-    python3 {baseDir}/scripts/notes_to_audio.py <project_path> --provider cosyvoice --voice-id <voice>
-    python3 {baseDir}/scripts/notes_to_audio.py <project_path> --provider ark-api
-    python3 {baseDir}/scripts/notes_to_audio.py <project_path> --provider ark-agent-plan
+    python3 {baseDir}/scripts/notes_to_audio.py <project_path>
+    python3 {baseDir}/scripts/notes_to_audio.py <project_path> --voice-id <voice_id>
     python3 {baseDir}/scripts/notes_to_audio.py --list-common-voices
     python3 {baseDir}/scripts/notes_to_audio.py --list-voices --locale zh-CN

 Dependencies:
-    python3 -m pip install edge-tts
-    ELEVENLABS_API_KEY=<key> for --provider elevenlabs
-    MINIMAX_API_KEY=<key> for --provider minimax
-    QWEN_API_KEY or DASHSCOPE_API_KEY=<key> for --provider qwen
-    COSYVOICE_API_KEY or DASHSCOPE_API_KEY=<key> for --provider cosyvoice
-    ARK_TTS_API_KEY=<Doubao Speech X-Api-Key> for --provider ark-api
-    ARK_AGENT_PLAN_API_KEY=<key> for --provider ark-agent-plan
+    ARK_AGENT_PLAN_API_KEY=<key>
 """

 from __future__ import annotations
@@ -86,16 +71,7 @@ class AudioJob:

 def _load_tts_env_file() -> None:
     """Load TTS-related keys from the first .env file, without overriding shell env."""
-    load_prefixed_env_file((
-        "ELEVENLABS_",
-        "MINIMAX_",
-        "QWEN_",
-        "DASHSCOPE_",
-        "COSYVOICE_",
-        "TTS_",
-        "ARK_TTS_",
-        "ARK_AGENT_PLAN_",
-    ))
+    load_prefixed_env_file(("ARK_AGENT_PLAN_", "TTS_VOICE"))


 def spoken_text(markdown: str) -> str:
@@ -378,30 +354,12 @@ async def _generate_edge_jobs(
 def main() -> int:
     _load_tts_env_file()

-    default_provider = backend_volcengine.configured_profile("edge") or "edge"
-
     parser = argparse.ArgumentParser(
         description=__doc__,
         formatter_class=argparse.RawDescriptionHelpFormatter,
     )
     parser.add_argument("project_path", type=Path, nargs="?")
     parser.add_argument("-o", "--output", type=Path, default=None)
-    parser.add_argument(
-        "--provider",
-        choices=[
-            "edge",
-            "elevenlabs",
-            "minimax",
-            "qwen",
-            "cosyvoice",
-            *backend_volcengine.SUPPORTED_PROFILES,
-        ],
-        default=default_provider,
-        help=(
-            "audio generation backend (default: TTS_BACKEND/TTS_PROVIDER, "
-            "otherwise edge)"
-        ),
-    )
     parser.add_argument(
         "--voice",
         default=None,
@@ -548,6 +506,7 @@ def main() -> int:
     parser.add_argument("--list-voices", action="store_true", help="query provider voices and exit")
     parser.add_argument("--locale", default=None, help='filter --list-voices by locale, e.g. "zh-CN"')
     args = parser.parse_args()
+    args.provider = backend_volcengine.FIXED_PROFILE

     if args.list_common_voices:
         backend_edge.print_common_voices()
@@ -866,9 +825,7 @@ def main() -> int:
                         output_path,
                         profile=backend_volcengine.TtsProfile(
                             name=backend.provider,
-                            provider=backend_volcengine.TTS_BACKEND_PROFILES[
-                                backend.provider
-                            ]["provider"],
+                            provider=backend_volcengine.FIXED_PROVIDER,
                             api_key=backend.api_key,
                             base_url=backend.base_url,
                             model=backend.model,
